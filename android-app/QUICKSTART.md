# 🚀 Быстрый старт - Schengen Calculator Android

## Запуск за 5 минут

### 1. Открыть проект
```bash
cd /home/dalibu/Development/eu_schengen_stay_calculator/android-app
# Откройте папку android-app в Android Studio
```

### 2. Синхронизировать Gradle
Android Studio автоматически предложит синхронизацию.
Или вручную: `File → Sync Project with Gradle Files`

Gradle загрузит все зависимости (~5 минут при первом запуске).

### 3. Запустить приложение
- Подключите Android устройство или создайте эмулятор
- Нажмите ▶️ Run (или `Shift + F10`)

### 4. Что уже работает
✅ Главный экран с расчетами
✅ Выбор референсной даты
✅ Отображение результатов 90/180
✅ Предупреждения о превышении
✅ Светлая/темная тема
✅ Локализация DE/EN

### 5. Что добавить первым делом

#### Быстрый тест - добавьте иконки
В Android Studio:
1. `File → New → Image Asset`
2. Выберите тип: Launcher Icons (Adaptive and Legacy)
3. Выберите изображение или используйте клипарт
4. Нажмите Next → Finish

Иконка будет автоматически создана для всех разрешений.

#### Реализуйте экран Stays
Скопируйте код из `DEVELOPMENT.md` раздел 1:
- Создайте `ui/stays/StaysViewModel.kt`
- Создайте `ui/stays/StaysScreen.kt`
- Добавьте в навигацию

## Структура проекта

```
android-app/
├── app/
│   ├── src/main/
│   │   ├── java/com/schengen/calculator/
│   │   │   ├── data/          # База данных и репозитории
│   │   │   ├── domain/        # Бизнес-логика
│   │   │   ├── ui/            # UI компоненты
│   │   │   │   ├── home/      # ✅ Готов
│   │   │   │   ├── stays/     # ⏳ TODO
│   │   │   │   ├── planning/  # ⏳ TODO
│   │   │   │   └── theme/     # ✅ Готов
│   │   │   ├── MainActivity.kt
│   │   │   └── SchengenApplication.kt
│   │   ├── sqldelight/        # SQL схема
│   │   └── res/               # Ресурсы (strings, themes)
│   └── build.gradle.kts
├── README.md          # Полное описание
├── DEVELOPMENT.md     # План разработки
└── PROJECT_SUMMARY.md # Отчет о проделанной работе
```

## Основные файлы для изучения

### Начните отсюда:
1. `README.md` - общий обзор
2. `PROJECT_SUMMARY.md` - что уже сделано
3. `DEVELOPMENT.md` - что делать дальше

### Ключевые классы:
1. `domain/SchengenCalculator.kt` - вся логика расчетов
2. `ui/home/HomeScreen.kt` - пример UI на Compose
3. `data/repository/StayRepository.kt` - работа с БД

## Полезные команды

### Gradle
```bash
# Собрать проект
./gradlew build

# Запустить тесты
./gradlew test

# Очистить
./gradlew clean

# Собрать APK
./gradlew assembleDebug
```

### Git
```bash
# Инициализировать репозиторий
git init
git add .
git commit -m "Initial Android app structure"
```

## Требования

- ✅ Android Studio Hedgehog (2023.1.1)+
- ✅ JDK 17
- ✅ Android SDK 34
- ✅ Минимум: Android 8.0 (API 26)

## Помощь

### Проблемы с Gradle?
```bash
# Очистить кэш
./gradlew clean --refresh-dependencies
```

### Ошибки компиляции?
1. Проверьте JDK версию: `File → Project Structure → SDK Location`
2. Синхронизируйте Gradle: `File → Sync Project with Gradle Files`
3. Invalidate Caches: `File → Invalidate Caches / Restart`

### Нужна помощь?
- Смотрите `DEVELOPMENT.md` для примеров кода
- Смотрите `PROJECT_SUMMARY.md` для архитектуры

---

**Приятной разработки!** 🚀
